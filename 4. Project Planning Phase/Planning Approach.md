The project followed a step-by-step development process. Initially, the dataset was collected and analyzed to understand its structure. After preprocessing the data, multiple machine learning models were trained and evaluated. The best-performing model was then saved and integrated into a Flask web application. Finally, the application was tested using different input values before preparing the documentation and demonstration.